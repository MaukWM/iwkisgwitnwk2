TARGET grammar point(s) — the sentence must exercise all of these:
{targets}

Learner's grammar bank — extracted from their own 1-1 production sentences, with per-point
sentence counts (context, not a level indicator or ceiling):
{bank}

Recent practice sentences for this target — the new sentence must differ from all of them:
{history}

Topic seed: {topic}

Candidate vocabulary (use 1-2 if natural, skip if forced):
{vocab}
